# دليل لوحة التحكم وإدارة المحتوى - مركز لؤي سعادة

يحتوي هذا الملف على إرشادات تفصيلية للمبرمج لتشغيل وإدارة لوحة التحكم الخاصة بموقع مركز لؤي سعادة لصيانة وفحص السيارات.

---

## 1. رابط لوحة التحكم

* الرابط المحلي أثناء التطوير: `http://localhost:3000/admin`
* رابط الإنتاج الحالي: `https://loay-saadeh-garage.pages.dev/admin`

> [!IMPORTANT]
> صفحة لوحة التحكم محجوبة عن محركات البحث باستخدام وسم `noindex, nofollow` ولا يوجد أي رابط عام يشير إليها داخل الموقع حفاظاً على الخصوصية.

---

## 2. متغيرات البيئة المطلوبة في Cloudflare Pages

لكي تعمل لوحة التحكم والقدرة على الكتابة المباشرة في GitHub بنجاح، يجب إضافة المتغيرات التالية في إعدادات مشروع **Cloudflare Pages** (تحت تبويب **Settings -> Environment variables**):

| المتغير | الوصف | مثال / القيمة الافتراضية |
| :--- | :--- | :--- |
| `ADMIN_PASSWORD` | كلمة مرور صفحة تسجيل الدخول للوحة التحكم | `password_here` (قم بتغييرها) |
| `SESSION_SECRET` | مفتاح تشفير عشوائي لتأمين وحفظ الجلسة (HttpOnly Cookie) | `any_long_random_string` |
| `GITHUB_TOKEN` | الـ Token الخاص بصلاحية الكتابة والتعديل على GitHub (مفصل أدناه) | `ghp_xxxxxxxxxxxx` |
| `GITHUB_OWNER` | اسم المستخدم أو المؤسسة المالكة للمستودع على GitHub | `idreessc-cmd` |
| `GITHUB_REPO` | اسم مستودع المشروع (Repository) | `loay-saadeh-garage` |
| `GITHUB_BRANCH` | الفرع (Branch) المستخدم للنشر | `main` |
| `SITE_DATA_PATH` | مسار ملف البيانات المركزي داخل المشروع | `data/site-data.json` |

---

## 3. طريقة إنشاء GitHub Token بصلاحيات محدودة (Fine-grained Personal Access Token)

لتأمين المشروع، يوصى بإنشاء Token ذو صلاحيات محدودة جداً ومقتصرة على مستودع الموقع فقط:

1. اذهب إلى حسابك في GitHub ثم **Settings -> Developer settings -> Personal access tokens -> Fine-grained tokens**.
2. اضغط على **Generate new token**.
3. قم بتسميته (مثال: `Cloudflare Pages - Loay Saadeh Garage`).
4. حدد الصلاحية الزمنية (Expiration) حسب رغبتك.
5. في قسم **Repository access**، اختر **Only select repositories** ثم اختر مستودع المشروع: `loay-saadeh-garage`.
6. في قسم **Permissions -> Repository permissions**:
   * ابحث عن **Contents** وحدد الصلاحية **Read and write**.
7. انقر على **Generate token** في الأسفل، ثم انسخ الـ Token الناتج وضعه في متغير البيئة `GITHUB_TOKEN` في لوحة تحكم Cloudflare Pages.

---

## 4. طريقة الحفظ وماذا يحدث بعد الحفظ

1. عند الدخول للوحة التحكم (`/admin`) وإدخال كلمة المرور، يتم إنشاء جلسة مشفرة باستخدام HMAC-SHA256 وتخزينها في ملف تعريف ارتباط آمن (`HttpOnly Cookie`).
2. قم بإجراء التعديلات المطلوبة في أي من التبويبات المتوفرة.
3. انقر على زر **"حفظ ونشر التغييرات"** في الأسفل.
4. ستظهر لك نافذة تأكيد تحذيرية، عند النقر على **"تأكيد ونشر"** سيحدث التالي:
   * تقوم Cloudflare Pages Function بالتحقق من البيانات وحسابات الدخول.
   * يتم الاتصال بـ GitHub API تلقائياً للحصول على الـ `sha` الحالي لملف البيانات وكتابة التحديث الجديد بالكامل.
   * تُنشئ عملية الحفظ Commit في الفرع المحدد برسالة: `update site content from admin dashboard`.
   * يقوم GitHub بإشعار Cloudflare Pages بوجود تحديث جديد، مما يطلق **عملية بناء ونشر تلقائية جديدة (Deployment)**.
   * يستغرق التحديث بين 2 إلى 3 دقائق ليصبح متاحاً للزوار على الويب تلقائياً.

---

## 5. النسخ الاحتياطي (Backup)

* توفر لوحة التحكم خياراً فورياً لتنزيل نسخة احتياطية من البيانات الحالية.
* انقر على زر **"تحميل نسخة احتياطية"** في الشريط العلوي لتنزيل ملف بصيغة `.json` يحتوي على كامل تفاصيل المحتوى.
* يمكنك حفظ هذا الملف للعودة إليه أو استخدامه في استعادة البيانات عند الحاجة.

---

## 6. إدارة الصور عبر لوحة التحكم (Images)

توفر اللوحة تبويباً مستقلاً باسم **"إدارة الصور"** يمكنك من تغيير مسارات جميع صور الموقع دون الحاجة لتعديل الكود.

### 6.1. الصور المتاحة حالياً

| الصورة | الاستخدام | المفتاح في JSON |
| :--- | :--- | :--- |
| صورة السيارات الكهربائية | بطاقة "السيارات الكهربائية" في صفحة أنواع السيارات | `images.electric` |
| صورة سيارات الهايبرد | بطاقة "سيارات الهايبرد" في صفحة أنواع السيارات | `images.hybrid` |
| صورة سيارات البنزين والديزل | بطاقة "سيارات البنزين والديزل" في صفحة أنواع السيارات | `images.defaultCar` |
| صورة Open Graph | الصورة التي تظهر عند مشاركة رابط الموقع في واتساب، فيسبوك، تلغرام... | `images.ogImage` |
| أيقونة الموقع (Favicon) | الأيقونة الصغيرة في تبويب المتصفح | `images.favicon` |

### 6.2. كيف أغير صورة؟

1. اذهب إلى لوحة التحكم (`/admin`)
2. سجل الدخول بكلمة المرور
3. اختر تبويب **"إدارة الصور"** من القائمة الجانبية
4. ستظهر لك بطاقات لكل صورة تحتوي على:
   * **معاينة حية** للصورة الحالية
   * **حقل نصي** يحتوي على المسار الحالي للصورة
5. غيّر المسار في الحقل النصي
6. اضغط على زر **"حفظ ونشر التغييرات"** في الأسفل
7. سيتم تحديث الملف على GitHub → Cloudflare Pages يعيد النشر تلقائياً

### 6.3. أين أضع الصور إذا كانت داخل `public`؟

إذا كانت الصورة موجودة داخل مجلد `public` في المشروع:
* اكتب المسار مبتدئاً بـ `/` (شرطة مائلة)
* مثال: `/my-new-image.png`
* مثال: `/images/car-electric.jpg`

الملفات داخل `public` تُنسخ كما هي إلى جذر الموقع المنشور.

### 6.4. كيف أستخدم رابط صورة خارجي؟

يمكنك استخدام رابط خارجي كامل مباشرةً:
* اكتب الرابط كاملاً مبتدئاً بـ `https://`
* مثال: `https://example.com/images/my-car.jpg`
* المعاينة في لوحة التحكم ستعرض الصورة من الرابط الخارجي

### 6.5. ما الفرق بين صورة الموقع وصورة Open Graph؟

* **صورة الموقع**: تظهر داخل الصفحة نفسها (مثل بطاقات أنواع السيارات)، ويراها الزوار أثناء تصفح الموقع.
* **صورة Open Graph**: لا تظهر داخل الموقع أبداً. تظهر فقط عند مشاركة رابط الموقع في تطبيقات مثل واتساب، فيسبوك، وتلغرام لتكون الصورة المصغرة المرفقة مع الرابط.

### 6.6. رفع صورة جديدة عبر لوحة التحكم

1. اذهب إلى تبويب **"إدارة الوسائط"**
2. اختر الصورة التي تريد تغييرها من بطاقات **"الصور الأساسية"**
3. انقر على زر **"رفع صورة"**
4. اختر ملف الصورة من جهازك (JPG, PNG أو WebP - حد أقصى 2MB)
5. انتظر حتى ينتهي الرفع
6. ستظهر رسالة نجاح، وسيتم تحديث المسار تلقائياً في الحقل النصي
7. سيتم حفظ الصورة في مجلد `public/uploads/` داخل GitHub وعمل commit تلقائي
8. Cloudflare Pages سيعيد النشر تلقائياً (2-3 دقائق)

### 6.7. مكتبة الصور

تبويب **"إدارة الوسائط"** يحتوي على قسم **"مكتبة الصور"** يعرض جميع الصور المسجلة حالياً في بيانات الموقع مع إمكانية نسخ رابط كل صورة.

### 6.8. سجل التغييرات

كل تغيير على الصور يتم تسجيله في قسم **"آخر التغييرات على الصور"** والذي يحفظ آخر 5 تغييرات مع:
* تاريخ التغيير
* اسم الصورة
* المسار القديم والجديد
* زر **"استرجاع"** للعودة إلى المسار السابق

### 6.9. آلية عمل رفع الصور

1. عند رفع صورة، يتم إرسالها إلى Cloudflare Function (`/api/admin/upload`)
2. الـ Function تتحقق من صلاحية الجلسة (نفس نظام تسجيل الدخول)
3. تتحقق من نوع الملف وتوقيعه الفعلي (JPG, PNG وWebP فقط)
4. تتحقق من حجم الملف (الحد الأقصى 2MB)
5. تُنشئ اسم ملف آمن: `{key}-{YYYYMMDD}-{HHMM}.{ext}`
6. ترفع الصورة + تحديث `site-data.json` في commit واحد إلى GitHub باستخدام GitHub Git Data API
7. ترجع المسار الجديد للواجهة

### 6.10. أين تحفظ الصور؟

تحفظ الصور داخل مستودع GitHub في المسار:
```
public/uploads/
```
مثال: `public/uploads/electric-20260629-1530.webp`

### 6.11. أنواع الصور المسموحة والحد الأقصى

| النوع | الامتداد | مسموح؟ |
| :--- | :--- | :--- |
| JPEG | .jpg / .jpeg | ✅ |
| PNG | .png | ✅ |
| WebP | .webp | ✅ |
| SVG | .svg | ❌ لأسباب أمنية |
| GIF, BMP, TIFF | .gif / .bmp / .tiff | ❌ |

الحد الأقصى لحجم الملف: **2MB**

### 6.12. ملاحظات مهمة

* إذا أدخلت مساراً خاطئاً، سيظهر مربع المعاينة فارغاً (لن تظهر الصورة). يمكنك إعادة المسار الصحيح وحفظه مرة أخرى.
* يمكنك استخدام رابط خارجي كامل (https://...) بدلاً من رفع صورة، فقط اكتب الرابط في حقل المسار.
* زر **"استرجاع الافتراضي"** يعيد المسار الأصلي للصورة.
* صورة Open Graph يُفضل أن تكون بحجم 1200×630 بكسل للحصول على أفضل مظهر عند المشاركة.
* الصور الاختيارية (Logo, Hero) إذا تركت فارغة لا تؤثر على تصميم الموقع.

---

## 7. حماية تسجيل الدخول في Cloudflare

يوجد قفل برمجي داخل Function بعد 5 محاولات فاشلة لمدة 15 دقيقة. ولأن ذاكرة Pages Functions ليست مخزناً موزعاً دائماً، يجب إضافة قاعدة Rate Limiting من لوحة Cloudflare عندما يتم ربط نطاق رسمي بالمشروع:

1. افتح **Security → WAF → Rate limiting rules** للنطاق.
2. أنشئ قاعدة باسم `Protect admin login`.
3. استخدم المسار `/api/admin/login` والطريقة `POST` إن كانت الخطة تدعم فلترة الطريقة.
4. اجعل خاصية العد عنوان IP.
5. الحد المقترح: 5 طلبات خلال دقيقة واحدة.
6. الإجراء: Managed Challenge أو Block لمدة 10 إلى 15 دقيقة.

> نطاق `pages.dev` تديره Cloudflare ولا يظهر كمنطقة DNS مستقلة في حساب العميل؛ لذلك تصبح قاعدة WAF على مستوى النطاق متاحة بعد شراء نطاق رسمي وربطه بالمشروع.

## 8. النطاق وSEO

العنوان المعتمد حاليًا هو `https://loay-saadeh-garage.pages.dev`. لا تغيّر `metadataBase` أو `canonical` أو `sitemap.xml` إلى نطاق مخصص إلا بعد التأكد من تسجيله وظهور سجلات DNS واستجابة HTTPS بنجاح.
